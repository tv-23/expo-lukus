---
import "../../public/assets/style/section-title.css";
const { title } = Astro.props;
---

<section class="section-title">
    <div class="mask-wrap">
        <h2 class="expo-lukus-title" set:html={title} />
        <br>
        <p  class="expo-lukus-title">Lorem ipsum dolor sit, amet consectetur adipisicing elit. Autem voluptate aliquid porro dolore quibusdam explicabo optio, commodi aut magni.</p>
    </div>
</section>

<script>
    gsap.registerPlugin(ScrollTrigger);

    gsap.utils.toArray(".section-title").forEach((section) => {
        const title = section.querySelectorAll(".expo-lukus-title");

        gsap.from(title, {
            y: 200,
            duration: 1.5,
            ease: "expo.out",
            scrollTrigger: {
                trigger: section,
                start: "top 85%",
                toggleActions: "play none none none",
            },
        });
    });
</script>
